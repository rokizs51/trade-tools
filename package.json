{
  "name": "trade-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node scripts/serve.mjs",
    "test": "npm run build && node test/calculateCosting.test.mjs && node test/persistence.test.mjs && node test/warnings.test.mjs && node test/sqliteRepository.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
